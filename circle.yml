machine:
  java:
      version:
        oraclejdk8
dependencies:
  override:
    - ./gradlew buildNeeded
compile:
  override:
    - ./gradlew build
test:
  override:
    - ./gradlew jacocoTestReport
    - bash <(curl -s https://codecov.io/bash)