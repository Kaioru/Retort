machine:
  java:
      version:
        oraclejdk8
dependencies:
  override:
    - ./gradlew buildDependents
compile:
  override:
    - ./gradlew build
test:
  override:
    - ./gradlew jacocoTestReport
    - bash <(curl -s https://codecov.io/bash)